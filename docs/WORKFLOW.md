# 系统任务流程详解

## 📋 完整任务流程图

```
┌─────────────────────────────────────────────────────────────────┐
│                      用户输入                                    │
│  python main.py [template.xlsx] [--output xxx --sheet xxx]      │
└──────────────────────┬──────────────────────────────────────────┘
                       ↓
┌─────────────────────────────────────────────────────────────────┐
│                   main.py 参数解析                               │
│  - excel_file (可选，插件可自动查找)                             │
│  - trace_file (可选，插件可自动查找)                             │
│  - output_file (可选)                                           │
│  - sheet_name (可选)                                            │
└──────────────────────┬──────────────────────────────────────────┘
                       ↓
┌─────────────────────────────────────────────────────────────────┐
│              workflow.process_log_to_excel()                     │
│  初始化 context = {excel_file, trace_file, output_file, ...}    │
└──────────────────────┬──────────────────────────────────────────┘
                       ↓
┌─────────────────────────────────────────────────────────────────┐
│                 plugins.load_plugins()                           │
│  加载并实例化所有启用的插件，按 level 排序                        │
└──────────────────────┬──────────────────────────────────────────┘
                       ↓
┌─────────────────────────────────────────────────────────────────┐
│              plugins.run_plugins(context)                        │
│  按层级顺序执行插件，每个插件的输出合并到 context               │
└──────────────────────┬──────────────────────────────────────────┘
                       ↓
        ╔══════════════════════════════════════════╗
        ║    插 件 执 行 流 程 (5个层级)            ║
        ╚══════════════════════════════════════════╝
                       ↓
┌─────────────────────────────────────────────────────────────────┐
│  Level 0: dld_configtmp 插件 - 模板准备                         │
│  ┌───────────────────────────────────────────────────────┐     │
│  │ 输入: context.get("excel_file")                       │     │
│  │ 处理:                                                 │     │
│  │   - 如果用户提供了模板 → 直接使用                     │     │
│  │   - 否则自动查找:                                     │     │
│  │     1. templates/*.xlsx                              │     │
│  │     2. examples/templates/*.xlsx                     │     │
│  │ 输出:                                                 │     │
│  │   context["dld_configtmp"] = {                       │     │
│  │     "template_path": "/path/to/template.xlsx",       │     │
│  │     "version": "...",                                │     │
│  │     "source": "user|auto"                            │     │
│  │   }                                                  │     │
│  └───────────────────────────────────────────────────────┘     │
└──────────────────────┬──────────────────────────────────────────┘
                       ↓
┌─────────────────────────────────────────────────────────────────┐
│  Level 1: config_parser 插件 - 配置解析                         │
│  ┌───────────────────────────────────────────────────────┐     │
│  │ 依赖: ["dld_configtmp"]                               │     │
│  │ 输入: context.get("trace_file")                       │     │
│  │ 处理:                                                 │     │
│  │   - 如果用户提供了trace → 直接解析                    │     │
│  │   - 否则自动查找:                                     │     │
│  │     1. logs/trace_*.txt                              │     │
│  │     2. logs/*.txt                                    │     │
│  │     3. examples/logs/*.txt                           │     │
│  │   - 使用正则解析trace文件提取配置块                   │     │
│  │   - 应用字段名映射                                    │     │
│  │   - 提取括号中的值(可选)                              │     │
│  │ 输出:                                                 │     │
│  │   context["config_parser"] = {                       │     │
│  │     "sections": [                                    │     │
│  │       {                                              │     │
│  │         "name": "opSch",                             │     │
│  │         "fields": {                                  │     │
│  │           "systemMode": "1",                         │     │
│  │           "debugLevel": "2",                         │     │
│  │           ...                                        │     │
│  │         }                                            │     │
│  │       },                                             │     │
│  │       ...                                            │     │
│  │     ],                                               │     │
│  │     "parser": <ConfigParser实例>,                    │     │
│  │     "trace_file": "/path/to/trace.txt"              │     │
│  │   }                                                  │     │
│  └───────────────────────────────────────────────────────┘     │
└──────────────────────┬──────────────────────────────────────────┘
                       ↓
┌─────────────────────────────────────────────────────────────────┐
│  Level 2: constraint_checker 插件 - 约束检查 ⭐                 │
│  ┌───────────────────────────────────────────────────────┐     │
│  │ 依赖: ["config_parser"]                               │     │
│  │ 输入: context["config_parser"]["sections"]            │     │
│  │ 处理:                                                 │     │
│  │   - 加载约束规则（版本管理）                          │     │
│  │   - 检查单组约束:                                     │     │
│  │     • only_allow: 字段值白名单                        │     │
│  │     • forbid: 字段值黑名单                            │     │
│  │     • only_allow_combinations: 字段组合约束           │     │
│  │   - 检查多组约束:                                     │     │
│  │     • same_value: 字段值一致性                        │     │
│  │     • sequence: 字段值序列                            │     │
│  │     • conditional: 条件约束                           │     │
│  │     • only_allow_combinations: 组间组合映射           │     │
│  │   - 收集所有违规信息                                  │     │
│  │ 输出:                                                 │     │
│  │   context["constraint_checker"] = {                  │     │
│  │     "validation_passed": True/False,                 │     │
│  │     "violations": [                                  │     │
│  │       {                                              │     │
│  │         "type": "only_allow",                        │     │
│  │         "rule": "规则名",                             │     │
│  │         "group": 0,                                  │     │
│  │         "field": "debugLevel",                       │     │
│  │         "value": "5",                                │     │
│  │         "allowed": ["0","1","2","3"],                │     │
│  │         "message": "详细错误信息"                      │     │
│  │       },                                             │     │
│  │       ...                                            │     │
│  │     ],                                               │     │
│  │     "version": "1.2.0_20240208"                      │     │
│  │   }                                                  │     │
│  └───────────────────────────────────────────────────────┘     │
└──────────────────────┬──────────────────────────────────────────┘
                       ↓
┌─────────────────────────────────────────────────────────────────┐
│  Level 3: excel_writer 插件 - Excel写入                         │
│  ┌───────────────────────────────────────────────────────┐     │
│  │ 依赖: ["dld_configtmp", "config_parser",              │     │
│  │        "constraint_checker"]                          │     │
│  │ 输入:                                                 │     │
│  │   - template_path (from dld_configtmp)               │     │
│  │   - sections (from config_parser)                    │     │
│  │   - validation_passed (from constraint_checker)      │     │
│  │ 处理:                                                 │     │
│  │   1. 加载Excel模板                                    │     │
│  │   2. 判断单/多 TopConfig 模式:                        │     │
│  │      - 单个TopConfig → 单工作表模式                   │     │
│  │      - 多个TopConfig:                                │     │
│  │        * multi_sheets模式 → 每组一个sheet            │     │
│  │        * single_sheet模式 → 所有组在一个sheet        │     │
│  │   3. 填充顶格表格 (top table)                         │     │
│  │   4. 填充子表 (sub tables):                           │     │
│  │      - 查找子表位置                                   │     │
│  │      - 匹配字段并填充                                 │     │
│  │      - 多个相同子表时复制模板                         │     │
│  │   5. 清理未使用的子表                                 │     │
│  │   6. 保存文件                                         │     │
│  │ 输出:                                                 │     │
│  │   context["excel_writer"] = {                        │     │
│  │     "output_file": "/path/to/output.xlsx",           │     │
│  │     "processor": <ExcelProcessor实例>,               │     │
│  │     "workbook": <Workbook对象>                       │     │
│  │   }                                                  │     │
│  └───────────────────────────────────────────────────────┘     │
└──────────────────────┬──────────────────────────────────────────┘
                       ↓
┌─────────────────────────────────────────────────────────────────┐
│  Level 4: auto_filename 插件 - 自动命名                         │
│  ┌───────────────────────────────────────────────────────┐     │
│  │ 依赖: ["excel_writer"]                                │     │
│  │ 输入:                                                 │     │
│  │   - output_file (from excel_writer)                  │     │
│  │   - processor (from excel_writer)                    │     │
│  │ 处理:                                                 │     │
│  │   1. 从顶格表格读取指定字段的值                       │     │
│  │   2. 应用值映射 (value_mapping)                       │     │
│  │   3. 组合成文件名后缀                                 │     │
│  │   4. 重命名输出文件                                   │     │
│  │      例: output.xlsx → output_Mode1_Level2.xlsx      │     │
│  │ 输出:                                                 │     │
│  │   context["auto_filename"] = {                       │     │
│  │     "output_file": "/path/to/renamed.xlsx"           │     │
│  │   }                                                  │     │
│  └───────────────────────────────────────────────────────┘     │
└──────────────────────┬──────────────────────────────────────────┘
                       ↓
┌─────────────────────────────────────────────────────────────────┐
│                   返回最终输出文件路径                            │
│  final_output = context.get("auto_filename", {})                │
│                 .get("output_file") or                          │
│                 context.get("excel_writer", {})                 │
│                 .get("output_file")                             │
└──────────────────────┬──────────────────────────────────────────┘
                       ↓
┌─────────────────────────────────────────────────────────────────┐
│                      完成 ✓                                      │
│  控制台输出:                                                     │
│    [Excel写入] ✓ 保存完成: /path/to/output.xlsx                 │
│  如果有约束违规:                                                 │
│    [约束检查] ✗ 发现 N 个违规                                    │
│    [1] 违规详情...                                              │
└─────────────────────────────────────────────────────────────────┘
```

---

## 🔄 数据流转详解

### Context 演进过程

```python
# 初始状态
context = {
    "excel_file": None,      # 用户输入（可选）
    "trace_file": None,      # 用户输入（可选）
    "output_file": None,     # 用户输入（可选）
    "sheet_name": None       # 用户输入（可选）
}

# ↓ Level 0: dld_configtmp 执行后
context = {
    ...原有字段,
    "dld_configtmp": {
        "template_path": "/path/to/template.xlsx",
        "version": "v1.0",
        "source": "auto"
    }
}

# ↓ Level 1: config_parser 执行后
context = {
    ...原有字段,
    "config_parser": {
        "sections": [
            {"name": "opSch", "fields": {...}},
            {"name": "ERCfg", "fields": {...}},
            ...
        ],
        "parser": <ConfigParser>,
        "trace_file": "/path/to/trace.txt"
    }
}

# ↓ Level 2: constraint_checker 执行后
context = {
    ...原有字段,
    "constraint_checker": {
        "validation_passed": False,
        "violations": [
            {
                "type": "only_allow",
                "message": "debugLevel=5 超出范围",
                ...
            }
        ],
        "version": "1.2.0_20240208"
    }
}

# ↓ Level 3: excel_writer 执行后
context = {
    ...原有字段,
    "excel_writer": {
        "output_file": "/path/to/output.xlsx",
        "processor": <ExcelProcessor>,
        "workbook": <Workbook>
    }
}

# ↓ Level 4: auto_filename 执行后
context = {
    ...原有字段,
    "auto_filename": {
        "output_file": "/path/to/output_Mode1_Level2.xlsx"
    }
}
```

---

## 🎯 关键决策点

### 1. 模板选择 (Level 0)

```
用户是否提供模板？
├─ 是 → 使用用户提供的模板
└─ 否 → 自动查找
    ├─ templates/*.xlsx 存在？
    │  ├─ 是 → 使用（选择最新的）
    │  └─ 否 → 继续
    └─ examples/templates/*.xlsx 存在？
       ├─ 是 → 使用（选择最新的）
       └─ 否 → 报错退出
```

### 2. Trace解析 (Level 1)

```
用户是否提供trace文件？
├─ 是 → 解析用户提供的trace
└─ 否 → 自动查找
    ├─ logs/trace_*.txt 存在？
    │  ├─ 是 → 使用（选择最新的）
    │  └─ 否 → 继续
    ├─ logs/*.txt 存在？
    │  ├─ 是 → 使用（选择最新的）
    │  └─ 否 → 继续
    └─ examples/logs/*.txt 存在？
       ├─ 是 → 使用（选择最新的）
       └─ 否 → 报错退出
```

### 3. 约束检查 (Level 2)

```
配置是否启用约束检查？
├─ 否 (enable: false) → 跳过，validation_passed=True
└─ 是 (enable: true)
    ├─ 加载规则版本
    ├─ 执行单组约束检查
    ├─ 执行多组约束检查
    └─ 返回检查结果
        ├─ 有违规 → validation_passed=False, violations=[...]
        └─ 无违规 → validation_passed=True, violations=[]
```

### 4. Excel写入模式 (Level 3)

```
有多少个TopConfig？
├─ 0个 → 只处理子表
├─ 1个 → 单工作表模式
│   ├─ 填充顶格表格
│   └─ 填充所有子表
└─ 多个 → 根据配置选择
    ├─ multi_top_mode: "multi_sheets"
    │   ├─ 为每个TopConfig创建一个sheet
    │   ├─ 复制模板到新sheet
    │   ├─ 填充各自的顶格表格和子表
    │   └─ 删除原模板sheet
    └─ multi_top_mode: "single_sheet"
        ├─ 在同一sheet中处理
        ├─ 只显示第一个TopConfig的顶格表格
        └─ 按顺序填充所有子表
```

### 5. 文件命名 (Level 4)

```
是否启用自动命名？
├─ 否 (enable: false) → 保持原文件名
└─ 是 (enable: true)
    ├─ 从顶格表格读取指定字段
    ├─ 应用值映射
    ├─ 生成后缀
    └─ 重命名文件
```

---

## ⚙️ 配置驱动流程

### 主配置 (config/default_config.yaml)

```yaml
# 不影响任务流，由插件内部使用
# 每个插件有自己的配置文件：
# - src/plugins/dld_configtmp/config.yaml
# - src/plugins/config_parser/config.yaml
# - src/plugins/constraint_checker/config.yaml
# - src/plugins/excel_writer/config.yaml
# - src/plugins/auto_filename/config.yaml
```

### 插件启用/禁用

```yaml
# 任意插件的 config.yaml
enable: true   # 启用插件
# 或
enable: false  # 禁用插件（会被跳过）
```

禁用插件的影响：
- 该插件不会执行
- 依赖该插件的其他插件也会被跳过
- 例如：禁用 config_parser → constraint_checker、excel_writer、auto_filename 都会被跳过

---

## 📊 执行日志示例

### 正常流程

```
============================================================
开始执行插件
============================================================

[Level 0] 执行插件: dld_configtmp
[模板下载] 未指定模板文件，自动查找...
[模板下载] ✓ 找到模板: templates/template.xlsx
  ✓ 完成

[Level 1] 执行插件: config_parser
[配置解析] 解析trace文件: logs/trace_001.txt
[配置解析] ✓ 找到 5 个配置块
  ✓ 完成

[Level 2] 执行插件: constraint_checker
[约束检查] 开始检查配置约束...
[约束检查] 使用规则版本: 1.2.0_20240208
[约束检查] ✓ 所有约束检查通过
  ✓ 完成

[Level 3] 执行插件: excel_writer
[Excel写入] 加载模板: templates/template.xlsx
[Excel写入] 使用工作表: Sheet1
[Excel写入] ✓ 顶格表格: 匹配 8 个字段
[Excel写入] ✓ 保存完成: templates/template_processed.xlsx
  ✓ 完成

[Level 4] 执行插件: auto_filename
  生成自动文件名...
  ✓ 重命名: template_processed.xlsx → template_Mode1_Level2.xlsx
  ✓ 完成

============================================================
插件执行完成
============================================================
```

### 有违规时

```
[Level 2] 执行插件: constraint_checker
[约束检查] 开始检查配置约束...
[约束检查] 使用规则版本: 1.2.0_20240208
[约束检查] ✗ 发现 2 个违规
  [1] [组1] 系统模式1的配置约束: 字段 'debugLevel' 的值 '5' 不在允许列表 ['0', '1', '2', '3'] 中
  [2] [组3] 系统模式1的配置约束: 字段 'dangerousFlag' 的值 '1' 在禁止列表中
  ✓ 完成

[Level 3] 执行插件: excel_writer
  ⚠️  跳过: 依赖未满足
```

**注意**：excel_writer 依赖 constraint_checker，如果约束检查失败但仍想继续写入Excel，需要修改 excel_writer 的 dependencies。

---

## 🔌 插件依赖关系

```
依赖链:
dld_configtmp (L0) - 无依赖
  ↓
config_parser (L1) - 依赖 dld_configtmp
  ↓
constraint_checker (L2) - 依赖 config_parser
  ↓
excel_writer (L3) - 依赖 dld_configtmp, config_parser, constraint_checker
  ↓
auto_filename (L4) - 依赖 excel_writer
```

**依赖检查规则**：
- 如果依赖插件未执行或执行失败 → 跳过当前插件
- 检查机制：在 context 中查找依赖插件的输出

---

## 🎛️ 自定义任务流

### 场景1：跳过约束检查

```yaml
# src/plugins/constraint_checker/config.yaml
enable: false
```

结果：
- constraint_checker 被跳过
- excel_writer 检查依赖时发现 constraint_checker 不在 context
- excel_writer 被跳过 ✗

**解决方案**：修改 excel_writer 的依赖
```python
# src/plugins/excel_writer/plugin.py
dependencies = ["dld_configtmp", "config_parser"]  # 移除 constraint_checker
```

### 场景2：只做约束检查，不写Excel

```yaml
# src/plugins/excel_writer/config.yaml
enable: false
```

结果：
- 执行到 Level 2（constraint_checker）后停止
- 输出约束检查结果
- 不生成Excel文件

### 场景3：使用自定义模板和trace

```bash
python main.py my_template.xlsx --trace my_trace.txt
```

结果：
- dld_configtmp 使用用户提供的模板
- config_parser 使用用户提供的trace
- 其他流程正常

---

## 🚀 性能特点

| 特性 | 说明 |
|------|------|
| **惰性执行** | 只执行启用的插件 |
| **依赖检查** | 依赖不满足时跳过插件 |
| **早期失败** | 关键插件失败时提前终止 |
| **上下文共享** | 避免重复加载文件 |
| **插件隔离** | 插件错误不影响其他插件 |

---

## 📝 总结

### 核心流程（5个步骤）

1. **准备模板** (dld_configtmp) → 找到或使用Excel模板
2. **解析配置** (config_parser) → 从trace提取配置数据
3. **检查约束** (constraint_checker) → 验证配置是否合规 ⭐
4. **写入Excel** (excel_writer) → 将数据填充到Excel
5. **重命名文件** (auto_filename) → 根据内容自动命名

### 关键特性

- ✅ **自动化**：自动查找模板和trace文件
- ✅ **可配置**：每个插件独立配置，可启用/禁用
- ✅ **可扩展**：插件架构，易于添加新功能
- ✅ **约束检查**：版本化的规则管理，支持复杂约束 ⭐
- ✅ **容错性**：依赖检查，失败时优雅降级

### 输入输出

**输入**：
- Excel模板（可选，可自动查找）
- Trace文件（可选，可自动查找）
- 输出文件名（可选）
- 工作表名（可选）

**输出**：
- 填充好数据的Excel文件
- 约束检查报告（如有违规）
- 自动生成的文件名（如启用）

---

**文档版本**: 1.0
**最后更新**: 2024-02-08
