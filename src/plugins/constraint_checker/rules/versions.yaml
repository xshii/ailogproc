# 约束规则版本索引

# 默认使用的版本（null 表示自动使用最新版本）
active_version: null

# 所有版本列表
versions:
  - version: "1.4.0"
    date: "2024-02-10"
    file: "v1.4.0_20240210.yaml"
    description: "统一架构版本：associate_by 关联约束、统一检查引擎、AST 安全表达式"
    features:
      - "associate_by 角色式关联：通过 where 条件识别 src1/src2/src3 角色"
      - "links 异构关联：支持同字段、跨字段、多字段联合匹配"
      - "支持线性链、扇入、扇出等拓扑结构"
      - "统一检查引擎：滑动窗口和关联约束共享同一套 same_value/sequence/conditional/combinations"
      - "validate 表达式：基于 AST 白名单的安全求值器（可选 CEL 引擎）"
    deprecated: false

# 版本命名规范:
# - 格式: vMAJOR.MINOR.PATCH_YYYYMMDD.yaml
# - MAJOR: 重大变更（不兼容的API更改）
# - MINOR: 功能新增（向后兼容）
# - PATCH: bug修复（向后兼容）
# - 日期: 创建日期 YYYYMMDD
